services:
  web-win:
    image: nginx:latest # Using the default Nginx image with custom configuration.
    volumes:
      # Mount the application code for live updates
      - .:/var/www
      # Mount the Nginx configuration file
      - ./docker-win/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      # Map port 80 inside the container to the port specified by 'NGINX_PORT' on the host machine
      - "8084:80"
    environment:
      - NGINX_HOST=localhost
    networks:
      - laravel-development
    depends_on:
      php-fpm-win:
        condition: service_started # Wait for php-fpm to start

  php-fpm-win:
    # For the php-fpm service, we will use our common PHP-FPM Dockerfile with the development target
    build:
      context: .
      dockerfile: ./docker-win/php-fpm/Dockerfile
      args:
        XDEBUG_ENABLED: ${XDEBUG_ENABLED:-true}
        XDEBUG_MODE: develop,coverage,debug,profile
        XDEBUG_HOST: ${XDEBUG_HOST:-host.docker.internal}
        XDEBUG_IDE_KEY: ${XDEBUG_IDE_KEY:-DOCKER}
        XDEBUG_LOG: /dev/stdout
        XDEBUG_LOG_LEVEL: 0
    env_file:
      # Load the environment variables from the Laravel application
      - .env
    volumes:
      # Mount the application code for live updates
      - .:/var/www
    networks:
      - laravel-development
    
  workspace-win:
    # For the workspace service, we will also create a custom image to install and setup all the necessary stuff.
    build:
      context: .
      dockerfile: ./docker-win/workspace/Dockerfile
      args:
        XDEBUG_ENABLED: ${XDEBUG_ENABLED:-true}
        XDEBUG_MODE: develop,coverage,debug,profile
        XDEBUG_HOST: ${XDEBUG_HOST:-host.docker.internal}
        XDEBUG_IDE_KEY: ${XDEBUG_IDE_KEY:-DOCKER}
        XDEBUG_LOG: /dev/stdout
        XDEBUG_LOG_LEVEL: 0
        DB_HOST: 192.168.111.8
        DB_PORT: 3306
        DB_USERNAME: db_cmit
        DB_DATABASE: db_cmit
        DB_PASSWORD: db_cmit
        CACHE_DRIVER: redis
        REDIS_CLIENT: predis
        REDIS_HOST: redis
        REDIS_PORT: 6379
    tty: true # Enables an interactive terminal
    stdin_open: true # Keeps standard input open for 'docker exec'
    env_file:
      - .env
    volumes:
      - .:/var/www
    networks:
      - laravel-development

  reverb-win:
    build:
      context: .
      dockerfile: ./docker-win/workspace/Dockerfile # Reutilizamos tu imagen que ya tiene PHP
    working_dir: /var/www
    command: php artisan reverb:start --host=0.0.0.0 --port=8085
    ports:
      - "8085:8085"
    volumes:
      - .:/var/www
    networks:
      - laravel-development
    depends_on:
      - workspace-win
      - redis-win

  redis-win:
    image: redis:alpine
    networks:
      - laravel-development

networks:
  laravel-development:
